# NoteDiscovery Configuration

app:
  name: "Vault of Obscenities"
  
server:
  host: "0.0.0.0"
  port: 8000
  reload: false
  allowed_origins: ["*"]
  debug: false
  
storage:
  notes_dir: "/home/ftr/Documents/Vault"
  plugins_dir: "./plugins"
  
search:
  enabled: true
  
authentication:
  enabled: true

  # Generated with: python3 -c "import secrets; print(secrets.token_hex(32))"
  secret_key: "b4b38c7d7b31d66d94048bcfc16c22846002e745299bca91dfe4b1d13e80f344"

  # Change this password! Run generate_password.py if you want a pre-hashed one.
  password: "admin"

  # Session expiry: 7 days
  session_max_age: 604800

